name: AQA Tests + Allure Report (GitHub Pages)

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 22
        uses: actions/setup-java@v4
        with:
          java-version: '22'
          distribution: 'temurin'

      # 1️⃣ RUN TESTS ONLY (generates allure-results)
      - name: Run tests
        run: mvn clean test -B

      # 2️⃣ INSTALL ALLURE CLI
      # Added 'if: always()' so this runs even if tests fail
      - name: Install Allure
        if: always()
        run: |
          curl -Ls https://github.com/allure-framework/allure2/releases/download/2.25.0/allure-2.25.0.tgz | tar -xz
          sudo mv allure-2.25.0 /opt/allure
          sudo ln -s /opt/allure/bin/allure /usr/bin/allure

      # 3️⃣ GENERATE HTML REPORT
      - name: Generate Allure Report
        if: always()
        run: |
          allure generate target/allure-results -o public --clean

      # 4️⃣ UPLOAD TO PAGES
      - name: Upload Pages artifact
        if: always()
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

      # 5️⃣ DEPLOY TO GITHUB PAGES
      - name: Deploy to GitHub Pages
        if: always()
        uses: actions/deploy-pages@v4